---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: helmreleasetests.testing.testing.platform.io
spec:
  group: testing.testing.platform.io
  names:
    kind: HelmReleaseTest
    listKind: HelmReleaseTestList
    plural: helmreleasetests
    singular: helmreleasetest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.helmReleaseRef.name
      name: HelmRelease
      type: string
    - jsonPath: .status.lastRunTime
      name: Last Run
      type: date
    - jsonPath: .status.lastRunResult
      name: Result
      type: string
    - jsonPath: .status.lastCommitSHA
      name: SHA
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HelmReleaseTest is the Schema for the helmreleasetests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HelmReleaseTest
            properties:
              cronJobRef:
                description: |-
                  cronJobRef is a reference to a suspended CronJob whose jobTemplate will be copied
                  to create test Jobs on each successful HelmRelease upgrade.
                properties:
                  name:
                    description: name is the name of the referenced object.
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the referenced object.
                      Defaults to the HelmReleaseTest's namespace if omitted.
                    type: string
                required:
                - name
                type: object
              helmReleaseRef:
                description: helmReleaseRef is a reference to the HelmRelease to watch
                  for successful upgrades.
                properties:
                  name:
                    description: name is the name of the referenced object.
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the referenced object.
                      Defaults to the HelmReleaseTest's namespace if omitted.
                    type: string
                required:
                - name
                type: object
              kustomizationRef:
                description: kustomizationRef is a reference to the Kustomization
                  used for commit SHA resolution.
                properties:
                  name:
                    description: name is the name of the referenced object.
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the referenced object.
                      Defaults to the HelmReleaseTest's namespace if omitted.
                    type: string
                required:
                - name
                type: object
            required:
            - cronJobRef
            - helmReleaseRef
            - kustomizationRef
            type: object
          status:
            description: status defines the observed state of HelmReleaseTest
            properties:
              conditions:
                description: conditions represent the current state of the HelmReleaseTest
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCommitSHA:
                description: lastCommitSHA is the commit SHA resolved from the Kustomization
                  at the time of the last test run.
                type: string
              lastRunJob:
                description: lastRunJob is the name of the most recently created test
                  Job.
                type: string
              lastRunResult:
                description: lastRunResult is the result of the most recent test Job
                  ("passed" or "failed").
                type: string
              lastRunTime:
                description: lastRunTime is the time the most recent test Job completed.
                format: date-time
                type: string
              lastTestedRevision:
                description: lastTestedRevision is the HelmRelease revision that was
                  last tested.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
